# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project

**My First Agent** — a project in the `Vaibhav Agents` workspace. This repository is
newly initialized and does not yet contain source code. Fill in the sections below as
the project takes shape.

## Current state

- Fresh git repository (single initial commit, no application code yet).
- `.claude/settings.local.json` holds local permission settings.
- Platform: Windows. Default shell is PowerShell; a Bash tool is also available for
  POSIX scripts.

## Getting started (to be filled in)

Once code is added, document the essentials here:

- **Setup** — how to install dependencies and configure the environment.
- **Build** — the command(s) to build the project.
- **Run** — how to start the agent / application locally.
- **Test** — how to run the test suite.

## Architecture (to be filled in)

Describe the high-level structure once it exists: the entry point, how the agent is
defined and invoked, key modules, and any external services or APIs it depends on.

## Conventions

- Match the style, naming, and structure of surrounding code as it grows.
- Keep this file up to date when adding tooling, commands, or notable architecture.

## Notes

- When building anything on the Claude API / Agent SDK, default to the latest, most
  capable Claude models (e.g. Opus 4.8, Sonnet 5, Haiku 4.5).
